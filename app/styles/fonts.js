import { Inika, Fredericka_the_Great } from 'next/font/google';
 
export const inika = Inika({
    weight: ['400'],
    subsets: ['latin']
});
 
export const frede = Fredericka_the_Great({
  weight: ['400'],
  subsets: ['latin'],
});